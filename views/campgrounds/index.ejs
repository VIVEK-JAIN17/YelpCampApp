<%- include('../partials/header') -%>
<header class="jumbotron">
    <div class="container text-center">
        <h1 class="display-3">Welcome to Yelp Camp!</h1>
        <p class="lead pt-4">View our hand-picked camp sites from all over the world!</p>
        <a class="btn btn-primary btn-lg mt-3" href="/campgrounds/new">Add new Campground</a>
    </div>
</header>
<div class="container-md text-center">
    <div class="row justify-content-center mb-3">
        <div class="col-md-10">
            <h3 class="dsplay-4">Our Most Popular Campgrounds</h3>
        </div>
    </div>
    <div class="row justify-content-around d-none d-md-flex">
        <% campgrounds.forEach(function(campground){ %>
        <div class="col-md-6 col-lg-4 my-3">
            <div class="card">
                <div class="card-img">
                    <img class="img-fluid" src="<%= campground.image %>" alt="<%= campground.name%>">
                </div>
                <div class="card-body">
                    <div class="card-title">
                        <h4> <%= campground.name %> </h4>
                    </div>
                    <div class="card-text">
                        <p><%= campground.description.substring(0,100) %>...</p>
                        <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary btn-sm">View More</a>
                    </div>
                </div>
            </div>
        </div>
        <%});%>
    </div>
    <div class="row justify-content-center d-flex d-md-none">
        <div class="my-4 mx-3 mx-sm-5 px-sm-3">
            <ul class="list-unstyled index">
                <% campgrounds.forEach(function(campground){ %>
                <li>
                    <div class="text-center align-self-center">
                        <img class="img-fluid pb-3" src="<%= campground.image %>" alt="<%= campground.name%>">
                    </div>
                    <div class="mb-4 text-left">
                        <header class="mb-2">
                            <h4 class="mb-1"> <%= campground.name %> </h4>
                            <div class="text-muted"> Posted By ~ <i> <%= campground.author.username %> </i></div>
                        </header>
                        <div>
                            <p><%- campground.description.substring(0,100) -%>...</p>
                            <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary btn-sm">View More</a>
                        </div>
                    </div>
                </li>
                <% }); %>
            </ul>
        </div>
    </div>
</div>
<%- include('../partials/footer') -%>